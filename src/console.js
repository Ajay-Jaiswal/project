        let findCartDetails= {items: 10}
        let totalQuantity = 0;
        for (let i in findCartDetails.items) {
           console.log(totalQuantity += findCartDetails.items[i].quantity)
        };
